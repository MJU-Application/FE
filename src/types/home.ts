export interface HotissueProp {
  date: string;
  text: string;
}

export interface HomeNoticeCardProp {
  title: string;
  date: string;
}

export interface MealCardProp {
  category: string;
  time: string;
  mealMenus: string[];
}
